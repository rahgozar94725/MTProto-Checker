*~

# Go
mtproto-checker.exe

# Generated in CI by git-cliff, never committed
RELEASE_NOTES.md

# Local-only helper script
filter.sh

# Default output of scripts/build-snapshot.mjs — real proxy links, built in CI, never committed
/snapshot.txt

# Agent-local notes, not shared with the team
.claude.local.md

# Agent process artifacts, kept on disk, never published.
# Durable decisions go in CLAUDE.md instead.
docs/superpowers/
.agent-docs/
tasks/

# JS test harness. Nothing here may live under public/ — //go:embed public
# would bake it into the release binary.
node_modules/
coverage/
test-results/
playwright-report/
.playwright/

# Archives are built outside the repository; this is the backstop.
*.zip
